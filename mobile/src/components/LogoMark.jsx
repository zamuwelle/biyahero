import Mark from '@/assets/logo-mark.svg'

/**
 * Biya-HERO. The cape is the whole idea — a jeepney with a hero cape lifting
 * behind it. Do NOT reuse this glyph for map pins: pins stay capeless so they
 * read at 44 px on a busy map.
 */
export const LogoMark = ({ size = 104 }) => <Mark width={size} height={size} />
